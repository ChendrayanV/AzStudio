<Window x:Class="AzStudio.App.Views.SendMessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Send Message" Height="360" Width="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False"
        FontFamily="{StaticResource AppFontFamily}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Subject (optional)" Style="{StaticResource FieldLabel}" Margin="0,0,0,2" />
        <TextBox x:Name="SubjectBox" Grid.Row="1" />

        <TextBlock Grid.Row="2" Text="Message body" Style="{StaticResource FieldLabel}" />
        <TextBox x:Name="BodyBox" Grid.Row="3" Margin="0,0,0,0"
                 AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Click="Cancel_Click" IsCancel="True" />
            <Button Content="Send" Click="Send_Click" IsDefault="True" Margin="0" />
        </StackPanel>
    </Grid>
</Window>
